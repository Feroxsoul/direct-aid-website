/**
 * Archived site has no footer section — component exists per project rules.
 * Renders nothing to avoid adding sections not present on the reference site.
 */
export function Footer() {
  return null;
}
